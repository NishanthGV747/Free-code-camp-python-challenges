def find_duplicates(arr):
    arr.sort()  # sort in place
    found = []
    
    for i in range(1, len(arr)):
        if arr[i] == arr[i-1] and (not found or found[-1] != arr[i]):
            found.append(arr[i])
    
    return found

        
